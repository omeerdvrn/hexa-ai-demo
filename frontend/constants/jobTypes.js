/**
 * Job types for different generation operations
 */
export const JobType = Object.freeze({
  LOGO: "logo",
});

/**
 * Array of all job types for validation
 */
export const JOB_TYPES = Object.freeze(Object.values(JobType));
